/** Module id. Must match the folder name under Foundry's `Data/modules/`. */
export const MODULE_ID = "pf2_display_exploration";

/** Root for i18n keys and template paths. */
export const TEMPLATE_ROOT = `modules/${MODULE_ID}/templates`;

/** Registered setting keys. */
export const SETTINGS = {
  activities: "activities",
  showToPlayers: "showToPlayers",
  panelPosition: "panelPosition"
};
